# Billing System

A simple billing system implemented in Python using SQLite for persistence.

## Usage

```bash
python -m billing.main add-customer "Alice" alice@example.com
python -m billing.main add-product "Widget" 10.0
python -m billing.main create-invoice 1
python -m billing.main add-item 1 1 2
python -m billing.main list
```
